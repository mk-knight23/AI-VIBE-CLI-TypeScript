/**
 * VIBE CLI - Provider Router (Backward Compatibility)
 * 
 * This file re-exports from the canonical providers/router.ts
 * for backward compatibility with existing imports.
 */

export { VibeProviderRouter, providerRouter } from '../../providers/router.js';
